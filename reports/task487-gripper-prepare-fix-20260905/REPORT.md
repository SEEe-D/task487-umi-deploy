# HOME 后夹爪准备恢复

- 19:37 运行 `20260905_193727_935322` 两次 HOME 成功，但准备失败于右 3.4254° / 左 7.1082°；后端双手持续 `stall=1`。尚未进入模型控制，这是执行准备问题。
- 后端需相对实测开度再张开超过 1°才解除锁存；原准备直接要求闭合到 1°，无法解除上一轮持物留下的锁存。
- `r` 现在先要求实测开度 +2°（限制在右 34°、左约 24.035°端点），实测到位容差 0.25°后再闭合到启动目标 1°。闭合容差最多 0.5°；每阶段最多等待 5 秒，任一失败冻结夹爪并阻止启动。
- 203 项离线测试通过，包含双手旧锁存恢复、单手张开失败、接近限位无法解锁的拒绝路径。未发送实机命令；实际解除锁存及放置效果待复测。
- 退出旧客户端，原命令重启；按 `r`，见到 `Grippers prepared` 后按 `d`。若仍夹着物体，先托住或移除：新的准备流程会先张开。模型服务及后端无需重启。

## 证据与实现

该会话 `gripper_feedback.csv` 首条双手开度为 3.4254°、7.1082°；
`gripper_ros_events.jsonl` 周期反馈双手 `stall=1`，保持目标与实测角一致。
客户端 19:37:50 和 19:38:15 两次准备失败。后端 `stall_release.hpp` 按实测位置判定张开请求，
安装配置要求超过 1°张开差。直接闭合不会解除旧锁存，HOME 不重启夹爪后端。

`prepare_grippers_for_runtime` 新增先张开解锁、后闭合准备两个阶段；本次先张开目标约 5.43°/9.11°。
准备不施加 5°补偿，模型参考与实体目标一起重置；无效反馈或超时冻结最后有效实测位置。
接近张开端点时可能无法取得足够解锁行程；若闭合不到位仍失败，不绕过准备检查。
力矩、速度、端点及机械臂保护保持原配置。

## 验证

完整 `tests_task487`：203 passed，2 项原有 SWIG DeprecationWarning，5.51 秒，见 `pytest.txt`。
新增模拟后端测试验证双手旧锁存、单手张开失败不进入闭合、近限位无法解除时冻结并拒绝。
复测按 `r` 后应依次见 `open-to-rearm reached`、`checkpoint-close reached`、`Grippers prepared`。
这次没有向真实机器人发送命令，软件验证不代表实物放置成功。
